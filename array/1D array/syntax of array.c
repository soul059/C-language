#include<stdio.h>

/*
    syntax of array
    Data_type name[size];
    Data_type name[size] = {x,y,z,..}; --> in this case size is not required!!
    Data_type name[rows][coluns];  --> this is for 2D array

    --> we can also initialize the array one by one accessing it using its index
*/
int main()
{
    int a[5];
    //    printf("a: %d\n",sizeof(a)); // sizeof gives us memory occupied by an object/variable
    //    printf("%u", &a[4]) gives us addres of given array
    //    printf("%d", a[4]) print an given index array
    return 0;
}
